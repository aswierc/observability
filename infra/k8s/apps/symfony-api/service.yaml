apiVersion: v1
kind: Service
metadata:
  name: symfony-api
  labels:
    app.kubernetes.io/name: symfony-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: symfony-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
